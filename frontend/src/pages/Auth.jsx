import {useState} from "react";import {useNavigate} from "react-router-dom";import {api,login} from "../api";
export default function Auth({mode="login"}){const[f,setF]=useState({name:"",email:"",password:"",role:"owner"}),[err,setErr]=useState(""),nav=useNavigate();const submit=async e=>{e.preventDefault();try{const d=await api(`/auth/${mode==="login"?"login":"register"}`,{method:"POST",body:JSON.stringify(f)});login(d);nav("/dashboard")}catch(e){setErr(e.message)}};return <main className="grid min-h-[calc(100vh-65px)] place-items-center px-4 py-10"><form onSubmit={submit} className="w-full max-w-md rounded-3xl bg-white p-7 shadow-lg ring-1 ring-slate-200"><h1 className="text-2xl font-black">{mode==="login"?"Welcome back":"Create your account"}</h1>{mode==="register"&&<input required placeholder="Full name" value={f.name} onChange={e=>setF({...f,name:e.target.value})} className="mt-6 w-full rounded-xl border p-3"/>}<input required type="email" placeholder="Email" value={f.email} onChange={e=>setF({...f,email:e.target.value})} className="mt-4 w-full rounded-xl border p-3"/><input required type="password" placeholder="Password" value={f.password} onChange={e=>setF({...f,password:e.target.value})} className="mt-4 w-full rounded-xl border p-3"/>{mode==="register"&&<select value={f.role} onChange={e=>setF({...f,role:e.target.value})} className="mt-4 w-full rounded-xl border p-3"><option value="owner">Shop owner</option><option value="influencer">Influencer</option></select>}{err&&<p className="mt-4 rounded-xl bg-red-50 p-3 text-sm text-red-700">{err}</p>}<button className="mt-5 w-full rounded-xl bg-violet-600 p-3 font-bold text-white">{mode==="login"?"Login":"Create account"}</button></form></main>}
